git_debug.log

.DS_Store

# Build artifacts (helper binaries + event providers are compiled on first run)
*.o
helpers/event_providers/cpu_load/cpu_load
helpers/event_providers/network_load/network_load
helpers/menus/menus
helpers/menus/bin/

# Wallpaper-adaptive theme runtime cache
theme/.cache/
theme/.watch.log
theme/.watch.pid

# App-icon font is built/installed separately via pnpm
scripts/
